## Phase 11 Review (carried into Phase 12 prompt)

- **Codex bridge adapter (`bridge/adapters/codex.js`):** mirrors `claude.js` / `gemini.js` structure exactly. `whichCodex` PATH lookup; `isAvailable()` returns `{available:false, reason}` until `which codex` succeeds AND either `~/.codex/auth.json` exists OR `OPENAI_API_KEY` env is set. `chat({messages, system}, sse, abortSignal)` spawns `codex exec --json "<prompt>"` with `NO_COLOR=1` and translates JSONL events. The translation table: `agent_message` / `agent_message_delta` → `sse.emitDelta(text)` (tolerating both `content.text` and flat `text` field for cross-patch tolerance), `task_complete` / `agent_message_done` → `sse.emitDone()`, `error` → `sse.emitError('CLI_CRASHED', 'codex reported an error event')` (PRIVACY: static structural message, never echoes `evt.message`). All other events (`tool_use`, `thinking`, `planner`, `session_start`, etc.) are dropped silently. Non-JSON lines on stdout (e.g. interleaved prctl warnings) skip without crash. Error-code map reuses the established `CLI_NOT_FOUND` / `CLI_CRASHED` / `AUTH_FAILURE` / `RATE_LIMITED` / `TIMEOUT` set so `TermuxBridgeBackend.mapBridgeErrorCode` (Phase 6) routes them to the correct preview-strip messaging — **no new branch needed in the Kotlin client**. `flattenMessages(messages, system)` folds multi-turn history into a single role-tagged prompt (`System: ...\n\nUser: ...\n\nAssistant: ...\n\nUser: ...`) because `codex exec` consumes one prompt per invocation; differs from `claude.js`'s `lastUserContent(messages)` so multi-turn context survives. Manual `killed` / `onAbort` / `cleanup` pattern matches `claude.js` / `gemini.js` for abort-signal handling. stderr is captured into a 2 KiB tail for `classifyCode` on non-zero exit but never echoed; the IME consumer only sees the static reason via the SSE code.

- **Adapter registry (`bridge/adapters/index.js`):** one-line addition of `codex: createCodexAdapter` to FACTORIES. `listProviders()` now returns `['claude', 'codex', 'gemini']`; `probeAll()` picks codex up automatically — `/providers` reports `{id:"codex", available:false, reason:"codex CLI not on PATH (run setup.sh)"}` until setup.sh runs.

- **`bridge/test/adapters.test.js` assertions updated:** the existing "lists claude and gemini" test renamed to "lists claude, codex, and gemini" with the expected sorted array `['claude', 'codex', 'gemini']`; the existing "returns null for unknown provider" test swapped from `createAdapter('codex')` to `createAdapter('nonexistent')` since codex is now registered. Both updates are part of Phase 11's PR; without them the prior assertions would have flipped from green to red.

- **`bridge/test/codex.test.js` (new, 20 tests):** mirrors `adapters.test.js`'s testing harness (`fakeSpawn` + `makeFakeChild` + `makeSseRecorder` + `fakeFs`). Pure-helper tests (`extractTextDelta` accept-list + reject-list, `isTerminalEvent`, `flattenMessages` 4 variants, `classifyCode` 401/429), `isAvailable` (4 paths: missing CLI, missing auth.json, both present, `OPENAI_API_KEY` env override), `chat` happy-path (canned transcript with init + 2 deltas + task_complete), `agent_message` non-delta single-shot, tool_use/thinking/planner suppression, non-JSON heartbeat tolerance, `error` event with static message (PRIVACY regression — asserts the prompt fragment from `evt.message` does NOT appear in SSE), nonzero-exit-code → `CLI_CRASHED`, prctl-style stderr regression (#6757) → `CLI_CRASHED`, spawn-error → `CLI_NOT_FOUND`. 45 bridge tests total now pass (25 prior + 20 new).

- **`setup.sh` extension to 11 steps:** new top-of-file constant `CODEX_VERSION="0.42.0"` with inline rationale (v0.43+ prctl regression openai/codex#6757). `SUPPORTED_PROVIDERS=(claude gemini codex)`. `parse_providers_arg` accepts `codex` (the prior `die "Codex is Phase 11"` stub removed). Renumbered all 9 `log_step "N/9"` strings to `N/11` and inserted two new steps: `4/11 DNS resolver shim` (new `ensure_resolv_conf` helper writes `nameserver 1.1.1.1` / `8.8.8.8` to `$PREFIX/etc/resolv.conf` only if absent or empty — Termux-wide fix for openai/codex#11809 that benefits any future Rust-based CLI) and `7/11 Codex CLI` (new `install_codex_if_selected`: `npm i -g @openai/codex@0.42.0` + post-install `codex --version` smoke check with explicit `prctl` regression error message if it fails). New `run_oauth_for_codex` calls the new `run_one_oauth_cmd` helper variant (which accepts an explicit subcommand argument — `codex login` vs Claude/Gemini's bare `claude` / `gemini`); checks `~/.codex/auth.json` for completion. `run_oauth_flows` (`8/11`) gained a codex branch. `--reauth` validator updated to accept `codex`; `run_reauth_only` case block extended. Interactive menu expanded from 3 options to 5, with "All three: Claude + Gemini + Codex" as the new default-on-Enter recommended pick (the prior `""|1` two-provider default was renamed to option 1 without the [recommended] label). Banner string mentions the Codex pin and the new DNS shim step. Final: `bash -n setup.sh` clean; `setup.sh --help` lists `claude, gemini, codex`.

- **`TermuxOrchestrator.Provider` (`app/.../ai/termux/TermuxOrchestrator.kt`):** added `CODEX("codex")` value alongside `CLAUDE("claude")` / `GEMINI("gemini")`. `reauthProvider(provider)` works automatically because the bash side now accepts `codex`. **No refactor on `TermuxBridgeBackend.kt`** — Phase 6's deliberate choice of `cliProvider: String` (vs an enum) means provider id strings round-trip through `/providers` without code changes. The single user-facing string-mapping site (`TermuxBridgeStatusScreen.displayName`) gained one branch (`"codex" -> "Codex"`) for proper capitalization in the status screen's per-provider rows; absent that, the fallback `else -> cliName` would render lowercase "codex" inconsistent with "Claude" / "Gemini".

- **`Provider.XAI_GROK` (`app/.../ai/client/Provider.kt`):** new enum value `XAI_GROK(displayName="xAI Grok", storageKey="xai_grok", defaultModel="grok-2-latest", apiKeyHelpUrl="https://console.x.ai")`. `Provider.entries` is iterated by `BackendsScreen`, so the new row appears automatically; `SecureStorage.getApiKey(...)` / `saveApiKey(...)` work without modification because they key by `storageKey: String`. Provider display name and help URL are inline string literals (Phase 3a precedent — no new string resources needed).

- **`RemoteApiBackend.streamGrok` (`app/.../ai/client/remote/RemoteApiBackend.kt`):** new sibling of `streamAnthropic` / `streamGemini`. Body parsing reuses Phase 10's `OpenAiCompatStreamParser.parseLine` via cross-package `internal` access (both packages live in the same Gradle module). The parser's `emit: (AiStreamEvent) -> Unit` callback is non-suspend; events are buffered into a local `mutableListOf` and re-emitted into the surrounding `FlowCollector<AiStreamEvent>` after `parseLine` returns. Channel-close without an explicit `[DONE]` sentinel emits `Done` (matches Anthropic path's fallback). `streamGrok` returns early on `AiStreamEvent.Done` so subsequent (post-`[DONE]`) bytes don't leak into the consumer.

  Two `when (provider)` expressions were extended for exhaustiveness: `rewrite()` (line 90) dispatches `XAI_GROK -> streamGrok(...)`, and `buildRequest()` (line 214) dispatches `XAI_GROK -> buildGrokRequest(...)`. Kotlin enforces exhaustive `when` expressions; missing either branch would compile-fail.

- **`buildGrokRequest` (companion-internal helper):** mirrors `buildAnthropicRequest` / `buildGeminiRequest` as a JVM-testable static. URL hardcoded to `https://api.x.ai/v1/chat/completions`. Headers: `Authorization: Bearer <key>` + `Content-Type: application/json` + `Accept: text/event-stream`. Body via `LocalLanRequestBuilder.openAiBody(model, messages)` where `messages` come from `LocalLanRequestBuilder.buildMessages(input, systemPrompt, fewShots)` — Phase 10's shared helper, so Grok's wire format matches `LocalLanBackend`'s OpenAI-compat path exactly. `model` parameter resolves from `RemoteApiBackend(provider, storage, model = provider.defaultModel)` = `"grok-2-latest"`.

- **`network_security_config.xml`:** added `<domain includeSubdomains="true">api.x.ai</domain>` to the existing HTTPS-only `<domain-config cleartextTrafficPermitted="false">` block (alongside `api.anthropic.com` and `generativelanguage.googleapis.com`). `<base-config cleartextTrafficPermitted="true">` (Phase 10 trade-off for user-configurable LocalLan) and its `tools:ignore="InsecureBaseConfiguration"` suppression are untouched. Verified the compiled `res/xml/network_security_config.xml` in the fdroid release APK reports `domain="api.x.ai"` via `aapt2 dump xmltree`.

- **`GrokRequestTest.kt` (new, 6 tests):** pure-JVM coverage matching the `RemoteApiBackendTest` pattern (no Robolectric, no Ktor instantiation). Tests: URL pinned to `api.x.ai/v1/chat/completions`; `Authorization: Bearer <key>` header set with no duplicate `x-api-key` leak; body shape decodes to `{model, stream:true, messages:[system,user]}`; few-shots inline as `user→assistant→user→assistant→user(final)`; empty `systemPrompt` omits the system message entirely; `Provider.XAI_GROK.defaultModel == "grok-2-latest"` + `storageKey == "xai_grok"` contract pin (so a future rename doesn't silently change the wire string).

- **Built / lint:** `assembleFdroidDebug assemblePlayDebug assembleFdroidRelease assemblePlayRelease` all clean. `lintFdroidDebug lintPlayDebug` produce 4 warnings — all pre-existing carry-overs from Phase 10 (`AndroidGradlePluginVersion` 8.14→8.14.5, `GradleDependency` Compose BOM 2025.11.01→2026.05.00, `NewerVersionAvailable` colorpicker-compose 1.1.3→1.1.4, `ObsoleteSdkInt` Phase 4 carry-over `TermuxValidationActivity.kt:199`). `git diff app/app/lint-baseline.xml` empty — no new entries. Lint hints 2 "LintBaselineFixed" entries (`AndroidGradlePluginVersion`, `GradleDependency`) noted but not material — they're orphaned baseline lines from older AGP / dep versions that no longer match; not introduced or removed in Phase 11. **157 AI-module JVM unit tests pass** (151 prior + 6 new `GrokRequestTest`). **45 bridge unit tests pass** (25 prior + 20 new `codex.test.js` — 11 happy path coverage + 9 error / privacy / regression coverage). 6 pre-existing Robolectric SDK-36 failures unchanged (Phase 12 polish task).

- **Dex invariants verified via `apkanalyzer dex packages` / `apkanalyzer dex code --class`:**
  - **fdroid release:** `com.aikeyboard.app.ai.client.Provider` class present with all three enum values (`ANTHROPIC` + `GOOGLE_GEMINI` + `XAI_GROK` — verified in `<clinit>` bytecode via `const-string "XAI_GROK"` + `const-string "xai_grok"`). `com.aikeyboard.app.ai.termux.TermuxOrchestrator$Provider` class present with all three (`CLAUDE` + `GEMINI` + `CODEX` — verified via `const-string "codex"` + `const-string "CODEX"`). `streamGrok` reachable from `RemoteApiBackend` (visible as `access$streamGrok` synthetic + `RemoteApiBackend$streamGrok$1` continuation class). `buildGrokRequest` not visible in dex (R8 inlines all three `build*Request` companion helpers into `buildRequest()`'s `when` branches — same R8 behavior for `buildAnthropicRequest` / `buildGeminiRequest` since Phase 3a, so no regression).
  - **play release:** identical to fdroid for Phase 11 surface — `Provider.XAI_GROK`, `TermuxOrchestrator$Provider.CODEX`, `streamGrok` all present. Phase 11 is entirely flavor-agnostic (no a11y dependency, same as Phase 9a/9b/10).
  - **NSC bundled XML:** `apkanalyzer resources value --name network_security_config` resolves to `res/8G.xml`; `aapt2 dump xmltree --file res/8G.xml` confirms `<domain includeSubdomains="true">api.x.ai</domain>` is in the `cleartextTrafficPermitted="false"` block.
  - **Manifest invariants:** `apkanalyzer manifest permissions` on both release APKs returns the **exact same permission set as Phase 10**: fdroid `{INTERNET, RECEIVE_BOOT_COMPLETED, VIBRATE, READ_USER_DICTIONARY, WRITE_USER_DICTIONARY, READ_CONTACTS, RUN_COMMAND, FOREGROUND_SERVICE, FOREGROUND_SERVICE_SPECIAL_USE, POST_NOTIFICATIONS}`; play `{INTERNET, RECEIVE_BOOT_COMPLETED, VIBRATE, READ_USER_DICTIONARY, WRITE_USER_DICTIONARY, READ_CONTACTS, RUN_COMMAND}` plus the standard `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`. **ZERO new `<uses-permission>` entries.** No new `<service>` / `<provider>` / `<receiver>` / `<activity>` entries.

- **Privacy invariants:** new files audited via `grep "Log\\."`:
  - `RemoteApiBackend.kt`: zero `Log.*` calls (unchanged from prior phases — the file has never logged).
  - `bridge/adapters/codex.js`: zero log calls of any kind. stderr is captured but **never echoed** (used only for `classifyCode` substring match → static error code mapping; the SSE consumer sees a static structural reason via `classifyStderr` summary that joins the last 3 stderr lines — bounded, never the full body). The `type:error` event branch uses a **static structural message** (`"codex reported an error event"`) explicitly to prevent leakage of `evt.message` (which on context-window-exceeded errors can contain a fragment of the user's prompt). Test `codex adapter emits error code on type:error event without echoing message` is the pinned regression assertion.
  - `setup.sh`: existing privacy posture preserved (no API key / token / prompt content logged; `run_one_oauth_cmd` passes stdio through to the user's terminal but the script itself doesn't capture or log).

- **Smoke test:** **deferred for human reviewer.** Phase 11 requires Termux + a real Codex device-code login + the bridge running to verify the codex path end-to-end, plus an xAI API key to verify the Grok path against `api.x.ai`. The 10 scenarios in PHASE_11_PROMPT.md §12 (fresh Codex install with resolv.conf shim, Codex chat happy path, Codex unauthenticated `/providers` payload, prctl regression smoke, `--reauth codex`, Grok happy path, Grok no-key, Grok auth-failure, privacy logcat for Grok, privacy logcat for Codex bridge) all need on-device verification. Build / lint / dex / manifest / NSC / unit-test invariants verified above.

- **Deviations from Phase 11 prompt:**
  - **Codex adapter error codes reuse the established `CLI_CRASHED` / `CLI_NOT_FOUND` / `AUTH_FAILURE` / `RATE_LIMITED` / `TIMEOUT` set** rather than the prompt's `CODEX_ERROR` / `CODEX_EXIT_NONZERO` / `CODEX_SPAWN_FAILED` codes. Reason: `TermuxBridgeBackend.mapBridgeErrorCode` (Phase 6, app/.../client/termux/TermuxBridgeBackend.kt:253) maps known codes to typed `ErrorType` values for preview-strip messaging (`RATE_LIMITED` → "rate limit hit; try again", `AUTH_FAILURE` → "API key invalid or expired", etc.); the prompt's `CODEX_*` codes would all fall through to `UNKNOWN`, losing the user-facing routing. Established codes preserve the precedent and the consumer experience.
  - **Codex adapter does NOT emit Done after Error** in any error branch. The prompt's §4 skeleton has `sse.emitError(...); sse.emitDone();` on close-with-error and spawn-error paths. `claude.js` / `gemini.js` precedent (verified at `claude.js:163-165`, `gemini.js:138-141`) emits Error and lets `server.js`'s `finally { sse.end() }` close the SSE. The IME-side consumer (`TermuxBridgeBackend.streamBridgeSseAsFlow`) treats `Error` as terminal and returns from the flow, so a trailing `Done` would be ignored. Aligning with the established pattern keeps the three adapters interchangeable in behavior.
  - **Codex spawn uses `stdio: ['ignore', 'pipe', 'pipe']`** (vs the prompt's `{signal: abortSignal}` spawn option). `codex exec --json <prompt>` takes the prompt via argv, not stdin, so stdin is ignored. The manual `killed` / `onAbort` / `cleanup` pattern matches `claude.js` / `gemini.js` rather than the `AbortSignal`-as-spawn-option style; both work on Node ≥ 15, but the codebase precedent is manual handling and unit tests are wired for it.
  - **`extractTextDelta` tolerates both `content.text` and flat `text` field.** The prompt's §4 example reads `evt.content?.text ?? evt.text`; this is preserved in the adapter. Minor patch revisions of v0.42.x may emit either shape; pinning both lets the adapter survive minor schema drift without a bridge release.
  - **`flattenMessages` for empty input.** Returns `""` when both `messages` and `system` are empty (covered by the test `flattenMessages handles empty messages array`). The `codex` subprocess invoked with an empty argv slot would still launch and exit early with a help message; emitting that to the user would surface as a CLI_CRASHED error. The bridge's `/chat` handler already rejects missing `messages` (Phase 4 `if (!Array.isArray(body.messages)) return reply.code(400).send(...)`), so the empty case only matters if the array is `[]` with non-string content. Acceptable v1.
  - **`setup.sh` step layout: 11 steps total, not the prompt's suggested 10.** Per §6.8 prompt offer ("`10/10 Start bridge service (was 8/9 + 9/9 merged, OR keep 9 and bump everything)`"), kept the existing 9 steps separate and added 2 new (DNS shim at 4/11, Codex install at 7/11) for a total of 11. Reason: "registering services" and "starting bridge + probing /health" are distinct failure modes; merging them obscures whether the failure is at `sv-enable` or at `/health` poll, which is the most common diagnostic question after a fresh-install OAuth.
  - **`run_one_oauth_cmd` introduced as a sibling to `run_one_oauth`.** The prompt's §6.5 example writes `codex_login_interactive` as its own function calling `codex login` directly. The existing `run_one_oauth` helper handles "skip if already authenticated" / "prompt user before launching" / "verify creds file after exit" — duplicating that logic in a one-off function would diverge over time. `run_one_oauth_cmd` is the same helper with an extra `subcommand` parameter, so all four CLIs (claude, gemini, codex, future-Phase-12) share the prompt-and-verify logic.
  - **`TermuxBridgeStatusScreen.displayName` gained a `"codex" -> "Codex"` branch** beyond the prompt's §7 ("Any UI strings the orchestrator references for CODEX (none expected, but verify when reading TermuxOrchestrator.kt)"). One-line UX consistency fix — without it the status screen shows lowercase "codex" alongside title-cased "Claude" / "Gemini". The prompt invited verification; this is the result.
  - **No R8 keep rule added for `streamGrok` or `buildGrokRequest`.** Per the prompt's §14 "3-for-3 precedent" advice ("expect-and-verify"), `apkanalyzer dex packages` after the build confirmed `streamGrok` is observable via `access$streamGrok` + `streamGrok$1` continuation class in both release APKs — no keep rule needed. `buildGrokRequest` IS inlined by R8 (same as `buildAnthropicRequest` / `buildGeminiRequest`); since Phase 3a never required a keep rule for those, Phase 11 follows the precedent.

- **Carried issues:**
  - On-device Phase 11 smoke test deferred to human reviewer (see Smoke test).
  - 6 pre-existing Robolectric SDK-36 failures unchanged from Phase 6/7a/7b/8/9a/9b/10.
  - All Phase 1/2/2.5/3a/3b/4/5a/5b/6/7a/7b/8/9a/9b/10 carry-overs unchanged: gesture lib not redistributable, `LocalLifecycleOwner` deprecation, `EncryptedSharedPreferences` deprecation in Phase 3a migration shim, localized HeliBoard brand strings, lint baseline 64 inherited entries, direct-boot migration corner case, `TermuxValidationActivity.kt:199` `ObsoleteSdkInt`, Phase 8 boot-persistence broken (BootReceiver does not result in re-posted FGS chip after reboot — Phase 12 polish task), WhatsApp validator file-existence check, multi-pack creation UX gap, pack tab background contrast, HeliBoard emoji-key gap, `bridge/README.md` still mentions only Claude + Gemini in the "Prerequisites" section (Phase 12 polish: add Codex to the prereq list now that the adapter ships).
  - **Phase 10 carry-over: NSC permissive `base-config`.** Phase 11 adds one more `<domain>` to the existing HTTPS-only block — does NOT touch `base-config`. The `tools:ignore="InsecureBaseConfiguration"` suppression remains the documented trade-off (LocalLanBackend is still the only backend whose URL is user-controlled).

- **PHASE_REVIEW.md alignment:** the per-phase Phase-11 row's "Done means" reads
  ```
  - bridge/adapters/codex.js wraps @openai/codex with stream-json IO
  - setup.sh provider menu includes Codex
  - IME provider list reflects Codex when authenticated
  - Grok: documented as deferred, API-key-only entry in RemoteApiBackend only
  ```
  All four items are realized exactly as written. The Phase 11 row in `ARCHITECTURE.md`'s build/phase plan is also matched ("OpenAI Codex adapter in bridge + IME provider entry; defer Grok pending CLI availability" — Grok still deferred from a CLI-via-Termux-bridge standpoint, but lands now as an API-key-only entry in `RemoteApiBackend`, matching PHASE_REVIEW.md's Phase 11 "Done means" Grok bullet).

- **Touchpoints for Phase 12 (polish + signed F-Droid build):**
  - **Codex version pinning re-evaluation.** Phase 12 release-prep should test the latest stable Codex on the user's actual Termux build; v0.42.0 was the conservative Phase 11 pin. The `CODEX_VERSION="0.42.0"` constant at the top of `setup/setup.sh` is the single source of truth — bump it when Phase 12 validates a newer working version.
  - **`bridge/README.md` mentions only Claude + Gemini.** The "Prerequisites" / sample-curl / endpoint-shape sections need a Codex row added.
  - **Codex event-name stability across versions.** v0.42.0's `agent_message` / `agent_message_delta` / `task_complete` event shape is the basis for `bridge/adapters/codex.js`'s parser. If Phase 12 unpins to a later version where event names changed, the adapter parser needs updating; the unit-test transcript fixtures will need new schema variants. Worth a smoke test against actual `codex exec --json` output before bumping.
  - **Grok model name evolution.** `grok-2-latest` is the v1 default. xAI has historically renamed models. Phase 12 polish can add a model-override field per provider in `BackendsScreen` (Anthropic and Gemini also use hard-coded defaults — same gap).
  - **OpenAI-compatible backend refactor.** Phase 10's `LocalLanBackend` (OPENAI_COMPATIBLE mode) and Phase 11's Grok branch both target the OpenAI-compat wire format. If Phase 12 adds a 4th provider (DeepSeek, Mistral La Plateforme), it'd be cleaner to factor a `OpenAiCompatRemoteBackend` taking (URL, model, key) — HTTPS-only version of `LocalLanBackend`. Defer until a real third API-key OpenAI-compat provider lands.
  - **`resolv.conf` shim collision.** If Termux ever ships with its own `resolv.conf`, our shim becomes a no-op (good) or conflicts (unlikely — file is checked for non-empty before write). Phase 12 release-prep should re-check against the current Termux release.
  - **Codex `OPENAI_API_KEY` env-var fallback in `isAvailable`.** The adapter accepts `OPENAI_API_KEY` env in lieu of `auth.json`, mirroring `claude.js` / `gemini.js` ANTHROPIC/GEMINI key handling. If `setup.sh` ever lands an env-key install path, the bridge would already work without re-deploy.

- **Open questions for human reviewer (carry into Phase 12 prompt):**
  1. **`codex` adapter error codes mapping.** The Phase 11 deviation reuses `CLI_CRASHED` / `CLI_NOT_FOUND` etc. instead of `CODEX_*`. If a future provider needs a provider-specific code (e.g., `CODEX_RATE_LIMITED_PER_MINUTE` distinct from RATE_LIMITED), `TermuxBridgeBackend.mapBridgeErrorCode` would need a new branch. Acceptable v1 to consolidate; revisit if smoke testing surfaces provider-specific error UX needs.
  2. **`flattenMessages` role tagging style.** The current shape (`System: ...\n\nUser: ...\n\nAssistant: ...\n\nUser: ...`) is one of several conventions. Codex's underlying GPT models historically respond well to natural-language role headers, but a deeper protocol (e.g., XML tags `<system>...</system><user>...</user>`) might preserve more context. Defer to Phase 12 smoke testing — if Codex's responses through the bridge differ meaningfully from direct `codex exec` invocations, the role-tag format is the first thing to revisit.
  3. **`resolv.conf` permissions.** `$PREFIX/etc/resolv.conf` is written by `setup.sh` running as the Termux user; default permissions are 644 in Termux. No sensitive content, but if a future Phase 12 release ships a Termux:Boot setup script that runs as a different user, it may not be able to read it. Worth a permissions audit during Phase 12 release-prep.
  4. **Grok auth-failure precision.** `RemoteApiBackend.mapHttpError(401)` returns `AUTH_FAILURE` with the upstream `error.message` if present, else "API key invalid or expired". xAI's 401 response body shape may differ from Anthropic / Gemini — `extractUpstreamErrorMessage` works against `{error:{message}}` (the OpenAI-compat shape), so it should pick up xAI's message verbatim. Worth verifying with a real xAI key during Phase 12 smoke.
  5. **Codex device-code OAuth UX.** The setup script does not parse the verification URL / code from Codex's stdout — it just prints the preamble and launches `codex login` interactively. If `codex login` ever ships a non-interactive form (e.g., `--no-tty` mode for headless setups), the Phase 5b TermuxOrchestrator could surface the device-code prompt inside the IME's wizard. Defer; the current Termux-foreground UX is the closest match to Claude's `claude.ai` browser handoff.
  6. **Multi-provider parallel install in `setup.sh`.** The script installs providers sequentially (`npm i -g claude` → wait → `npm i -g gemini` → wait → `npm i -g codex` → wait). A `--parallel` flag would speed this up for first-time installs at the cost of intertwined npm logs. Defer to Phase 12 polish unless smoke testing shows the sequential install is a real bottleneck.
  7. **xAI Grok model evolution policy.** `grok-2-latest` may become deprecated. Phase 12 should add a per-provider model-override field in `BackendsScreen` (same gap Anthropic / Gemini have). The Phase 11 `GrokRequestTest.modelDefaultsToGrok2LatestViaProviderEnum` test pins the contract for now; bumping it is a one-line enum change.

When DoD invariants hold, the prompt instructed: stop here, do not begin Phase 12.
