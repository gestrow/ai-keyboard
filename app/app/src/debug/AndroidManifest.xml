<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug-only manifest overlay.
    The com.termux.permission.RUN_COMMAND uses-permission and the
    <queries><package name="com.termux"/></queries> entry moved to
    src/main/AndroidManifest.xml in Phase 5b — TermuxOrchestrator fires
    RUN_COMMAND intents from release builds.
    What stays here is only the Phase 4 RUN_COMMAND validation harness
    activity, since it ships in debug builds only.
    SPDX-License-Identifier: GPL-3.0-only
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name="com.aikeyboard.app.ai.termux.TermuxValidationActivity"
            android:exported="true"
            android:label="Termux IPC Validation (debug)"
            android:theme="@style/platformActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
