<?xml version="1.0" encoding="utf-8"?>
<!--
    AI Keyboard network security config (Phase 3a).
    HTTPS-only for the two remote-API providers we ship with. Phase 4 adds a
    cleartext entry for 127.0.0.1 to talk to the Termux bridge; Phase 10 expands
    to user-configured RFC1918 ranges.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.anthropic.com</domain>
        <domain includeSubdomains="true">generativelanguage.googleapis.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
